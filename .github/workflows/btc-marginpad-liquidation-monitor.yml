name: BTC Liquidations · Hyperliquid via MarginPad

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/btc-marginpad-liquidation-monitor.yml"
      - "scripts/btc-marginpad-liquidation-monitor.js"
  schedule:
    - cron: "0 */5 * * *"

concurrency:
  group: btc-marginpad-liquidation-monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Run BTC liquidation monitor
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node scripts/btc-marginpad-liquidation-monitor.js
