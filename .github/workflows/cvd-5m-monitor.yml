name: BTC CVD 5M Monitor

on:
  push:
    paths:
      - 'scripts/cvd-5m-monitor.js'

permissions:
  contents: read

concurrency:
  group: btc-cvd-5m-monitor-v3
  cancel-in-progress: true

jobs:
  run:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CVD 5M monitor disabled"
