name: PolyBackTest Liquidity 5M (disabled)

on:
  workflow_dispatch:

# Disabled: volume and liquidity are now handled by
# polybacktest-volume-liquidity-5m.yml.
# Kept only to avoid deleting the historical workflow file.
