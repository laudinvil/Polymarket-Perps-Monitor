name: Polymarket Perps Cascade Exhaustion Monitor

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  monitor:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Perp Cascade Monitor disabled"
